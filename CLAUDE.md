# CLAUDE.md — プロジェクト設定

## プロジェクト概要

情報システムデザイン 最終課題リポジトリ。
メインプロジェクトは「図書館蔵書検索システム」（Flask + SQLite）。

## 学習記録の自動保存ルール

コードの説明・設計解説・教授セッション（「教えて」「説明して」「理解したい」などの依頼）が
一段落したら、会話の内容を **自動で** `学習記録/` ディレクトリに保存すること。

### ファイル命名規則
```
学習記録/YYYY-MM-DD_トピック名.md
例: 学習記録/2026-06-29_図書館システム設計解説.md
```

### 保存する内容
- 質問と説明のやり取り（Q&A形式）
- コードスニペットと解説
- 設計の思考の流れ・判断理由
- 図や対応表（Markdown で表現）

### 保存のタイミングと手順
ユーザーから「保存して」と言われなくても、説明セッションが終わったと判断したら
以下の手順を自動で実行する。

1. `学習記録/YYYY-MM-DD_トピック名.md` にセッション内容を保存
2. `git add 学習記録/` でステージ
3. `git commit -m "docs: add 学習記録/ファイル名"` でコミット
4. `git push origin main` でプッシュ
5. 完了後に「`学習記録/ファイル名.md` に保存・プッシュしました」と一言伝える。

## ディレクトリ構成

```
information_system_design/
├── CLAUDE.md                    ← このファイル
├── README.md                    ← 要求定義書
├── 図書館貸し出しシステム/        ← メイン実装（Flask）
│   ├── app.py                   ← プレゼンテーション層（ルーティング）
│   ├── models.py                ← アプリケーション層（ビジネスロジック）
│   ├── database.py              ← データ層（DB初期化・接続）
│   ├── requirements.txt
│   └── templates/               ← Jinja2 テンプレート
├── 学習記録/                     ← コード解説・やり取りの記録
├── sequence_diagram/            ← シーケンス図（PNG）
├── Collection_of_Diagrams/      ← 各種設計図
├── 講義資料/                     ← 授業PDF（git管理外）
└── _archive/                    ← ゲームプログラム（旧プロジェクト）
```

## 開発メモ

- 実行: `cd 図書館貸し出しシステム && python app.py`
- アクセス: http://127.0.0.1:5000
- DB は起動時に自動生成（`library.db`）、git管理外（.gitignore済み）
- ブランチ: `main`（本番）/ `OHASHI`（個人作業）/ `WADA`（チームメンバー）
